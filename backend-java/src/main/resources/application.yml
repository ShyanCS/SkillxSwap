spring:
  application:
    name: SkillSwapBackend
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: ${SERVER_PORT:8080}
  shutdown: graceful
  # Correct client IPs behind a reverse proxy / load balancer. Without this,
  # rate limiting would bucket every request under the proxy's IP.
  forward-headers-strategy: native
  error:
    include-message: never
    include-stacktrace: never

spring.lifecycle:
  timeout-per-shutdown-phase: 20s

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never

app:
  jwt:
    expiration-ms: ${JWT_EXPIRATION_MS:604800000} # 7 days
  otp:
    ttl-minutes: ${OTP_TTL_MINUTES:10}
  ratelimit:
    # Requests allowed per window, per client IP.
    otp-per-hour: ${RATELIMIT_OTP_PER_HOUR:5}
    login-per-15min: ${RATELIMIT_LOGIN_PER_15MIN:10}
    ai-per-hour: ${RATELIMIT_AI_PER_HOUR:30}
  ai:
    enabled: ${AI_ENABLED:false}
    api-key: ${GEMINI_API_KEY:}
    model: ${GEMINI_MODEL:gemini-2.0-flash}
    endpoint: ${GEMINI_ENDPOINT:https://generativelanguage.googleapis.com/v1beta/models}
  cloudinary:
    cloud-name: ${CLOUDINARY_CLOUD_NAME:}
    api-key: ${CLOUDINARY_API_KEY:}
    api-secret: ${CLOUDINARY_API_SECRET:}

logging:
  level:
    root: ${LOG_LEVEL_ROOT:INFO}
    com.skillswap.backend: ${LOG_LEVEL_APP:INFO}
  pattern:
    # requestId comes from RequestIdFilter (MDC) so logs for one request can be correlated.
    console: "%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [%X{requestId:-}] %logger{36} - %msg%n"
